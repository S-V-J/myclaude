# config.yaml
# ONE MODEL (Nemotron 3 Ultra) × 4 API KEYS = 4 deployments
# Each model name maps to a specific API key for load isolation
# Fallback: if PROJECT_2-4 keys not set, they fall back to PROJECT_1

litellm_settings:
  use_chat_completions_url_for_anthropic_messages: true
  num_retries: 5
  request_timeout: 3600
  master_key: os.environ/LITELLM_MASTER_KEY
  drop_params: true
  max_parallel_requests: 8
  cache: false
  log_level: "WARNING"
  # Better handling of NVIDIA rate limits
  routing_strategy: "least-busy"
  # Disable problematic features
  telemetry: false
  callbacks: []

general_settings:
  global_max_parallel_requests: 8
  request_timeout: 3600
  num_retries: 5
  health_check_interval: 300

router_settings:
  allowed_fails: 5
  retry_after: 10
  cooldown_time: 10
  routing_strategy: "least-busy"
  default_max_parallel_requests: 8
  # Retry settings for NVIDIA rate limits
  retry_on_rate_limit: true
  retry_on_timeout: true
  retry_after: 10

model_list:
  # Model 1: Default / Opus (1M context) → PROJECT_1
  - model_name: claude-opus-5
    litellm_params:
      model: nvidia_nim/nvidia/nemotron-3-ultra-550b-a55b
      api_key: os.environ/NVIDIA_API_KEY_PROJECT_1
      temperature: 1.0
      top_p: 0.95
      max_tokens: 16384
      drop_params: true
      # Better retry handling
      num_retries: 5
      timeout: 300
    model_info:
      mode: chat

  # Model 2: Sonnet → PROJECT_2 (fallback to PROJECT_1)
  - model_name: claude-sonnet-5
    litellm_params:
      model: nvidia_nim/nvidia/nemotron-3-ultra-550b-a55b
      api_key: os.environ/NVIDIA_API_KEY_PROJECT_2
      temperature: 1.0
      top_p: 0.95
      max_tokens: 16384
      drop_params: true
      num_retries: 5
      timeout: 300
    model_info:
      mode: chat
    fallback_model: claude-opus-5

  # Model 3: Sonnet 5 (1M context) → PROJECT_3 (fallback to PROJECT_1)
  - model_name: claude-sonnet-5-1m
    litellm_params:
      model: nvidia_nim/nvidia/nemotron-3-ultra-550b-a55b
      api_key: os.environ/NVIDIA_API_KEY_PROJECT_3
      temperature: 1.0
      top_p: 0.95
      max_tokens: 16384
      drop_params: true
      num_retries: 5
      timeout: 300
    model_info:
      mode: chat
    fallback_model: claude-opus-5

  # Model 4: Haiku → PROJECT_4 (fallback to PROJECT_1)
  - model_name: claude-haiku-4-5
    litellm_params:
      model: nvidia_nim/nvidia/nemotron-3-ultra-550b-a55b
      api_key: os.environ/NVIDIA_API_KEY_PROJECT_4
      temperature: 1.0
      top_p: 0.95
      max_tokens: 16384
      drop_params: true
      num_retries: 5
      timeout: 300
    model_info:
      mode: chat
    fallback_model: claude-opus-5
